#  <#Title#>

build with team code
97988GNC59


when you make a title of a snippet, that should appear in the content if the content is empty

remove action menu from main popup.  have a setting that allows a midoier key to be pressed while selecting a snippet, to bring up the action ment

make sure the main popup fits on the screen, if the mouse pointer is too low for it to all fit beneath, then have its top corner extend up as high as needed

